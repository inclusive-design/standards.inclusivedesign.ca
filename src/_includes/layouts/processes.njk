{% extends "layouts/base.njk" %}

{% set pageType = 'processes' %}

{% block content %}
<article class="flow">

	{% set stages = collections['stages_' + lang] %}
	{% for stage in stages %}
	<section class="wrapper">
	<h2>{{ stage.data.title }}</h2>
	{{ stage.content | safe }}
	<div class="cards">
		{% set processes = collections['processes_' + lang] %}
		{% for process in processes %}
		{% if stage.data.uuid === process.data.stage %}
			{% include 'partials/components/card--process.njk' %}
		{% endif %}
		{% endfor %}
	</div>
	</section>
	{% endfor %}

</article>
{% endblock %}
